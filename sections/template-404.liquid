<h1>{{ section.settings.title}}</h1>
<p>{{ section.settings.subtitle_title }}</p>

{% schema %}
{
    "name": "Template 404",
    "settings": [
            {
                "type": "text",
                "id": "title",
                "default": "404",
                "label": "404 Heading Title"
            },
            {
                "type": "text",
                "id": "subtitle_title",
                "default": "The page that you are looking for is not found.",
                "label": "404 Subtext Title"
            }
        ]
}
{% endschema %}